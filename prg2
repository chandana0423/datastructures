#include<stdio.h>
#include<conio.h>
int main()
{
	int num,i,n,
	fact=1;
	printf("enter the number:");
	scanf("%d",&num);
	for(i=1;i<=num;i++)
	{
	 fact=fact*i;
	}
	printf("the factorial value is:%d",fact);
	return 0;
}
	
